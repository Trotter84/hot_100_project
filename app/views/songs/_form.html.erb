<%= form_for [@artist, @song] do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <br>

  <%= f.label :billboard_id %>
  <%= f.select :billboard_id, [['No Billboard', nil]] + @billboards.collect { |b| [b.name, b.id] } %>

  <br>
  
  <%= f.submit %>

<% end %>